import { FilterCategoriaPipe } from './filter-categoria.pipe';

describe('FilterCategoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCategoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
